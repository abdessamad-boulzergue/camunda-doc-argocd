apiVersion: v1
kind: Service
metadata:
  name: {{ include "camunda-doc-gen-backend.fullname" . }}
  labels:
    {{- include "camunda-doc-gen-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "camunda-doc-gen-backend.selectorLabels" . | nindent 4 }}
