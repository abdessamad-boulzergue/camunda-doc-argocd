apiVersion: v1
kind: Service
metadata:
  name: {{ include "camunda-doc-gen.fullname" . }}
  labels:
    {{- include "camunda-doc-gen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "camunda-doc-gen.selectorLabels" . | nindent 4 }}
