apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: camunda-doc-gen
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/abdessamad-boulzergue/camunda-doc-argocd.git # TODO: Update this
    targetRevision: HEAD
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
